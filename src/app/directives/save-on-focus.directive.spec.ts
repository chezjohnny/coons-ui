import { SaveOnFocusDirective } from './save-on-focus.directive';

describe('SaveOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
