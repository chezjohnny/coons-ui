import { EditableOnFocusDirective } from './editable-on-focus.directive';

describe('EditableOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new EditableOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
